// Array

const ary1: number[] = [1, 2, 3, 4];

const ary2: (string | number)[] = ['naisan', 1];

let ary3: string[] = [];
    ary3.push('naisan');
    ary3.push('sakib');


// Object

const obj1: { name: string, role: number } = { name: 'naisan', role: 4 };

let obj2: { title: string, description: string, price: number };
    obj2 = {
        title: 'i am title',
        description: 'i am description',
        price: 200
    }
